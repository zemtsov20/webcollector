package com.common.enums;

public enum State {
        QUEUED,

        DOWNLOADING,
        DOWNLOADING_ERROR,
        DOWNLOADED,

        PARSING,
        PARSING_ERROR,
        PARSED,

        CHECKED
}
